// src/pages/MapPage.js
import React from "react";
import { Typography } from "@mui/material";

const MapPage = () => (
  <div>
    <Typography variant="h4">地圖頁面</Typography>
    <Typography>這是地圖頁面。</Typography>
  </div>
);

export default MapPage;
